server:
  port: 10086 # 服务端口
spring:
  application:
    name: eurekaserver  # 微服务的名称

# eureka本身也是一个微服务，在启动时会将自己也注册到eureka上
eureka:
  client:
    service-url: # eureka的地址信息
      defaultZone: http://127.0.0.1:10086/eureka